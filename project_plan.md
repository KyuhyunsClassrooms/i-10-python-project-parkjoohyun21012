# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제:전기차/ESS 배터리 팩 관리 및 수명 진단 시스템 (BMS 시뮬레이터)

예) 기분과 시간에 따른 활동 추천기

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

예) 사용자가 현재 기분과 사용할 수 있는 시간을 입력하면, 조건에 맞는 활동을 추천해 주고 싶다.

내 설명: 전기차와 에너지저장장치(ESS)에 사용되는 배터리 팩은 잔량(SOC)과 건강도(SOH)를 지속적으로 관리해야 한다. 배터리가 과충전되거나 과방전되면 안전 문제가 발생하고, 사이클이 누적될수록 수명이 줄어든다. 이 프로그램은 여러 배터리 팩의 데이터를 바탕으로 SOC와 SOH를 계산하고, 상태를 정상/주의/위험으로 진단해 경고 메시지와 수명 예측을 출력한다.


---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

- 2차원 리스트에 저장된 배터리 팩 데이터 (팩ID, 소재, 설계용량, 현재잔량, 누적사이클, 최대사이클수명)
- 사용자가 선택하는 팩 ID (상세 진단 시)
- 종료 명령 (q 입력)

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- SOC 계산: (현재잔량 / 설계용량) × 100
- SOH 계산: (1 - 누적사이클 / 최대사이클수명) × 100
- 조건문으로 상태 판정: SOC < 10% → 위험, SOC < 20% → 주의, SOH < 20% → 위험, SOH < 50% → 주의
- 반복문으로 모든 팩을 순회하며 진단 결과 생성

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

- 전체 배터리 팩 진단 리포트 (팩ID, 소재, SOC, SOH, 상태, 경고 메시지)
- 사용자가 선택한 팩의 상세 진단 결과 (잔량, 건강도, 잔여 사이클, 경고 메시지)

---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름:battery_packs

예) foods, movies, characters, questions, records

```python
# 예시 형식
battery_packs = [
    ["PACK-01", "LFP",    100, 87,  320, 3000],
    ["PACK-02", "NCM",    120, 45,  810, 1500],
    ["PACK-03", "전고체",  90, 91,   55, 5000],
    ["PACK-04", "LFP",   100,  8,  2890, 3000],
    ["PACK-05", "NCM",   120, 110,  430, 1500],
]
```

### 한 행의 의미
배터리 팩 하나의 현재 상태 정보 (팩 ID, 소재 종류, 용량, 잔량, 사이클 현황)

### 각 열의 의미

| 열 번호 | 의미 | 예시 |
|---:|---|---|
| 0번 열 | 팩 ID | "PACK-01" |
| 1번 열 | 소재 종류 | "LFP", "NCM", "전고체" |
| 2번 열 | 설계 용량 (Ah) | 100 |
| 3번 열 | 현재 잔량 (Ah) | 87 |
| 4번 열 | 누적 사이클 수 | 320 |
| 5번 열 | 최대 사이클 수명 | 3000 |
---

## 5. 함수 설계
|함수이름|역할-|입력값|변환값 또는 출력
calculate_soh(pack)SOH(건강도%) 계산팩 데이터 1행SOH 값 (실수)
calculate_soc(pack)SOC(잔량%) 계산팩 데이터 1행SOC 값 (실수)
diagnose_pack(pack)SOC·SOH 기반 상태 판정 및 경고 생성팩 데이터 1행soc, soh, 상태, 경고 메시지 목록
print_report(packs)전체 팩 진단 리포트 출력전체 배터리 팩 리스트   표 형식 출력
get_user_choice()사용자로부터 팩 ID 입력받기없음선택한 팩 ID 문자열
print_detail(pack_id)특정 팩 상세 진단 출력팩 ID 문자열 상세 정보 출력
main()전체 실행 흐름 제어 없음 없음

## 6. 사용할 제어구조

### 조건문을 사용할 부분

예) 사용자의 예산보다 가격이 낮은 항목만 추천한다.

내 계획:SOC가 100% 초과이면 과충전 위험으로 판정한다.
SOC가 10% 미만이면 과방전 위험으로 판정한다.
SOC가 20% 미만이면 잔량 부족 주의로 판정한다.
SOH가 20% 미만이면 수명 임박 위험으로 판정한다.
SOH가 50% 미만이면 노화 진행 주의로 판정한다.


### 반복문을 사용할 부분

예) 2차원 리스트의 모든 항목을 하나씩 검사한다.

내 계획:for 반복문으로 battery_packs의 모든 행을 순회하며 각 팩을 진단한다.
while True 반복문으로 사용자가 올바른 팩 ID를 입력할 때까지 반복해서 입력받는다.


---

## 7. 예상되는 예외 상황

예) 조건에 맞는 추천 결과가 없을 때, 잘못된 입력을 했을 때 등

1. 사용자가 존재하지 않는 팩 ID를 입력했을 때 → 오류 메시지 출력 후 재입력 요청

2. 현재 잔량이 설계 용량을 초과했을 때 (과충전) → SOC > 100%로 계산되어 위험 판정

3. 누적 사이클이 최대 수명을 초과했을 때 → SOH가 0% 이하로 내려가지 않도록 0으로 처리
---

## 8. Gemini에게 도움받고 싶은 부분

예) 주제 구체화, 2차원 리스트 구조, 함수 분리, 오류 수정 등

1. SOC·SOH 계산 공식이 실제 BMS 기준에 맞는지 확인
2. 조건문으로 상태를 정상/주의/위험 3단계로 분류하는 방법
3. f-string으로 표를 정렬해서 출력하는 방법
