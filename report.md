# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제: 전기차/ESS 배터리 팩 관리 및 수명 진단 시스템 (BMS 시뮬레이터)

이 프로그램을 만든 이유: 전기차와 에너지저장장치(ESS)에서 배터리는 안전과 직결되는 핵심 부품이다. 배터리의 잔량(SOC)과 건강도(SOH)를 실시간으로 감시하고 이상 상태를 경고하는 BMS는 에너지공학, 화학, 신소재공학이 모두 연결되는 분야다. 이 프로그램을 통해 실제 BMS의 핵심 기능을 파이썬으로 구현해보고, 배터리 소재별 수명 특성을 비교해보고 싶었다.




---

## 2. 문제 분해 및 모델링

### 입력
2차원 리스트에 저장된 배터리 팩 데이터 (팩ID, 소재, 설계용량, 현재잔량, 누적사이클, 최대사이클수명)
사용자가 입력하는 팩 ID (상세 진단 선택 시)

### 처리
SOC 계산: (현재잔량 / 설계용량) × 100
SOH 계산: (1 - 누적사이클 / 최대사이클수명) × 100
조건문으로 SOC·SOH 수치에 따라 정상/주의/위험 판정 및 경고 메시지 생성
반복문으로 전체 팩을 순회하며 진단 결과 수집

### 출력
전체 배터리 팩 진단 리포트 (표 형식, 상태 및 경고 메시지 포함)
사용자가 선택한 팩의 상세 진단 (잔량, 건강도, 잔여 사이클, 경고)



---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름:battery_packs

한 행의 의미:배터리 팩 하나의 현재 상태 정보 전체 (어떤 소재로 만들어졌고, 지금 잔량과 사이클이 얼마나 쌓였는지)

각 열의 의미:

| 열 번호 | 의미 | 실제 예시 |
|---:|---|---|
0번 열 팩 ID "PACK-01"
1번 열 소재 종류 "LFP", "NCM", "전고체"
2번 열 설계 용량 (Ah) 100
3번 열 현재 잔량 (Ah) 87
4번 열 누적 사이클 수 320
5번 열 최대 사이클 수명 3000
| 0번 열 |  |  |
| 1번 열 |  |  |
| 2번 열 |  |  |
| 3번 열 |  |  |

이 2차원 리스트를 사용한 이유:배터리 팩 여러 개를 하나의 리스트로 묶어서 관리할 수 있기 때문이다. 반복문으로 리스트를 순회하면 팩이 몇 개가 되든 같은 코드로 진단할 수 있어 효율적이다. 실제 데이터베이스처럼 행은 개체(팩), 열은 속성(특성)으로 구분하는 구조다.



---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| calculate_soc(pack) | SOC(잔량%) 계산 | 팩 데이터 1행 | SOC 값 (실수) |
|calculate_soh(pack)  |SOH(건강도%) 계산  | 팩 데이터 1행 | SOH 값 (실수) |
|diagnose_pack(pack)  |상태 판정 및 경고 생성  |  팩 데이터 1행| soc, soh, 상태, 경고 메시지 목록 |
|print_report(packs)  | 전체 팩 리포트 출력 |전체 팩 리스트  | 표 형식 출력 |
| get_user_input() |팩 ID 입력받기  |  없음 | 선택한 팩 ID 문자열 |
|print_detail(pack_id)  | 특정 팩 상세 진단 출력 |팩 ID 문자열  | 상세 정보 출력 |
| main() |전체 흐름 | 제어없음| 없음

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름:diagnose_pack(pack)

설명:이 함수는 배터리 팩 하나를 받아서 SOC와 SOH를 계산하고, 조건문으로 상태를 판정해 경고 메시지 목록과 함께 반환한다. SOC가 100%를 초과하면 과충전 위험, 10% 미만이면 과방전 위험, 20% 미만이면 잔량 부족 주의로 판정한다. SOH가 20% 미만이면 수명 임박 위험, 50% 미만이면 노화 진행 주의로 판정한다. 두 조건을 모두 검사해서 더 심각한 상태로 최종 판정을 결정한다. 이 함수가 없으면 나머지 출력 함수들이 동작하지 않기 때문에 프로그램의 핵심 함수다.


---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?
diagnose_pack() 함수 안에서 SOC와 SOH 수치를 기준으로 상태를 판정한다. SOC > 100이면 과충전 위험, SOC < 10이면 과방전 위험, SOC < 20이면 잔량 부족 주의로 분류한다. SOH < 20이면 수명 임박 위험, SOH < 50이면 노화 진행 주의로 분류한다. 또한 get_user_input()에서 사용자가 올바른 팩 ID를 입력했는지 조건문으로 검사한다.

### 반복문 사용 부분

무엇을 반복 처리했는가?
print_report() 함수에서 for 반복문으로 battery_packs의 모든 행을 순회하며 각 팩을 진단하고 출력한다. get_user_input() 함수에서 while True 반복문으로 사용자가 올바른 팩 ID를 입력할 때까지 반복해서 입력을 요청한다. main() 함수에서도 while True로 사용자가 종료(q)를 입력할 때까지 상세 진단을 반복한다.

### 조건문과 반복문이 함께 작동한 부분
print_report() 함수에서 for 반복문으로 전체 팩을 순회하면서, 각 팩마다 diagnose_pack()을 호출해 조건문으로 판정한 결과를 출력한다. 경고 메시지가 있는 경우에만(if w != "이상 없음") 경고를 출력하는 조건문도 반복문 안에서 함께 작동한다.

---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```text==============================================================
  전기차/ESS 배터리 팩 관리 및 수명 진단 시스템 (BMS)
==============================================================
  등록된 배터리 팩 수: 5개

==============================================================
     BMS 배터리 팩 진단 리포트
==============================================================
팩ID        소재          SOC    SOH     상태
--------------------------------------------------------------
PACK-01    LFP        87.0%  89.3%     정상
PACK-02    NCM        37.5%  46.0%     주의
           └ 배터리 노화 진행 (SOH < 50%)
PACK-03    전고체     101.1%  98.9%     위험
           └ 과충전 감지 (SOC > 100%)
PACK-04    LFP         8.0%   3.7%     위험
           └ 과방전 위험 (SOC < 10%)
           └ 배터리 수명 임박 (SOH < 20%)
PACK-05    NCM        91.7%  71.3%     정상
--------------------------------------------------------------

[전체 요약]  총 5팩  |  정상 2  주의 1  위험 2
즉시 점검 필요 팩: PACK-03, PACK-04

```

결과 분석:PACK-03은 현재 잔량(91Ah)이 설계 용량(90Ah)을 초과해 SOC가 101.1%로 계산되어 과충전 위험으로 판정됐다. PACK-04는 잔량이 8Ah로 SOC 8%에 불과하고 누적 사이클이 2890회로 최대 수명(3000회)에 근접해 위험 판정을 두 가지 동시에 받았다. PACK-02는 SOH 46%로 노화가 진행 중이어서 주의 판정을 받았다.




---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력: PACK-01 선택 (LFP, 잔량 87Ah, 사이클 320회)

예상 결과: SOC 87.0%, SOH 89.3%, 상태 정상, 경고 없음

실제 결과: 예상과 동일하게 출력됨


### 테스트 2: 예외 또는 경계 사례

입력: 존재하지 않는 팩 ID "PACK-99" 입력

예상 결과: 오류 메시지 출력 후 재입력 요청

실제 결과: "[오류] 올바른 팩 ID를 입력하세요." 메시지 출력 후 while 반복문으로 다시 입력 받음

### 발견한 오류와 수정한 방법

SOH 계산 시 누적 사이클이 최대 수명을 초과하면 SOH가 음수가 되는 문제를 발견했다. if soh < 0: soh = 0 조건을 추가해 최솟값을 0%로 처리하도록 수정했다.
---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:SOC와 SOH의 정의 및 계산 공식 확인, 2차원 리스트 열 구성 아이디어, f-string 정렬 출력 방법


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분: 전압 기반 SOC 계산(수행평가 수준에 맞지 않아 사이클 기반으로 단순화), tabulate 외부 라이브러리(수업 범위 외라 f-string으로 직접 구현), 온도·전압 열 추가(설명 복잡도가 높아져 제외)


최종 코드에서 내가 직접 판단하거나 수정한 부분:열 인덱스를 상수(ID=0, CAP_MAX=2 등)로 선언해 가독성을 높인 것, 경고 메시지를 트리 구조(└)로 출력하는 방식, SOH 음수 방지 처리, 위험/주의 중복 판정 시 더 심각한 상태를 유지하는 로직


---

## 9. 한계와 개선 방안

현재 프로그램의 한계: 현재 프로그램의 한계: 배터리 팩 데이터가 코드 안에 고정되어 있어 실시간으로 변하는 잔량이나 사이클을 반영하지 못한다. 또한 온도, 전압 등 실제 BMS에서 중요한 변수들이 빠져 있다. 소재별로 권장 SOC 범위가 다르지만(LFP: 20~90%, NCM: 20~80%) 이를 반영하지 못했다.


다시 개선한다면 추가하고 싶은 기능: 충전·방전 시뮬레이션 기능(잔량을 직접 변경해보기), 소재별 권장 SOC 범위 반영, 배터리 팩 데이터를 CSV 파일에서 불러오는 기능, 온도 변수 추가


---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
입력: 2차원 리스트의 배터리 팩 데이터, 사용자가 선택하는 팩 ID
처리: SOC·SOH 계산 → 조건문으로 상태 판정 → 경고 메시지 생성
출력: 전체 팩 진단 리포트, 선택한 팩 상세 진단

2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
한 행: 배터리 팩 하나의 현재 상태 정보 전체
열: 팩ID(0), 소재(1), 설계용량(2), 현재잔량(3), 누적사이클(4), 최대사이클(5)

3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
diagnose_pack(): SOC·SOH를 계산하고 조건문으로 정상/주의/위험을 판정해 경고 메시지와 함께 반환하는 함수


4. 조건문과 반복문은 어디에서 사용되었는가?
조건문: diagnose_pack()에서 SOC·SOH 수치 기반 상태 판정, get_user_input()에서 입력값 검증
반복문: print_report()의 for문으로 전체 팩 순회, get_user_input()과 main()의 while문으로 입력 반복

5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
Gemini: SOC·SOH 공식 확인, 2차원 리스트 열 구성 아이디어, f-string 정렬 방법
직접 수정: 열 인덱스 상수 선언, 경고 출력 형식, SOH 음수 방지 처리, 중복 상태 판정 로직
